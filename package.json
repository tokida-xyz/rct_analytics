{
  "name": "interaction-analysis-app",
  "version": "1.0.0",
  "description": "交互作用検定Webアプリ",
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "start": "cd backend && python -m uvicorn app.main:app --host 0.0.0.0 --port $PORT",
    "dev": "concurrently \"cd backend && uvicorn app.main:app --reload\" \"cd frontend && npm run dev\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
